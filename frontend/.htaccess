Options -MultiViews
RewriteEngine On
RewriteBase /

# Laisser passer les fichiers existants (assets, images, js, css)

RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Sinon, rediriger toutes les routes vers index.html

RewriteRule ^ index.html [L]
